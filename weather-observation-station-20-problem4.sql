SELECT CAST(DECIMAL(X.LAT_N, 10, 4) AS DECIMAL(10, 4)) FROM (SELECT LAT_N, ROW_NUMBER() OVER (ORDER BY LAT_N) AS RN, COUNT(*) OVER () AS C FROM STATION) AS X WHERE X.RN = CEIL(X.C / 2.0) OR X.RN = FLOOR(X.C / 2.0) + 1 ORDER BY X.LAT_N FETCH FIRST 1 ROW ONLY;
